{
  "name": "sermon-video-editor",
  "version": "1.0.0",
  "description": "Bookend a video with a PNG title card (crossfade in, crossfade out, fade to black) and render the final file.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "express": "^4.19.2",
    "multer": "^2.2.0"
  }
}
